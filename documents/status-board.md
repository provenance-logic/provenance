# Provenance — Status Board

> **What this is.** A one-screen snapshot of where Provenance actually stands. Read this *before* anything else when you sit down for a session. Regenerated by Claude at session end, or on demand. The authoritative per-feature detail lives in `prd/implementation-status.md`; this file is the dashboard view.

**Last updated:** 2026-05-22 late session (9 PRs on top of the day's 12 — ADR-010 step 1 closed end-to-end (agents.service.ts fix → audit → ESLint rule at ERROR), consumer-grade outbound reframe + per-port snippet generator + tool picker as the first consumer-grade primitive, B-068 marketplace cross-org **read** path unblocked via `@AllowCrossOrgRead`, B-070 / B-071 filed as PRD-input architectural findings. **The weekend input package is complete:** lift survey + reframe + service-org-filter audit + four candidate-Blocker bug entries (B-063, B-068 ✅ closed, B-070, B-071) all on main. **PRD overhaul starts 2026-05-23 morning.**

---

## Right now

**Stage:** Pre-PRD-overhaul. The OSR roadmap as previously sequenced no longer reflects reality. Stage 5 ("Pre-launch sweep") cannot honestly tag `v0.1.0-osr` while B-063 is open. The full PRD goes back to the drawing board on the 2026-05-24 weekend; this board is now a TRUTHFUL snapshot for that conversation, not a polished launch dashboard.

**Active OSR blockers:** **1** — [B-063](bugs/open.md#B-063). The connector framework advertises 12 connector types in the type system, the registration form, and the API. Only **3** have real implementations end-to-end: PostgreSQL (probe + schema), S3 (probe + schema), Databricks (probe + schema + discovery crawl + table-level lineage projection — Layers 1-4 shipped in PRs #142-#146 tonight). The other 9 (mysql, snowflake, bigquery, redshift, gcs, azure_blob, kafka, redpanda, rest_api, custom) still fall through `ConnectorProbeService.probe()`'s default branch that returns a synthetic `healthy` without doing anything. A first-time contributor registering a Snowflake or Kafka connector sees the same fakery the original B-063 filing documented. **OSR ≠ "one connector works"** — it means every advertised type does.

**Open High-severity bugs:** **2** — [B-070](bugs/open.md#B-070) (inbound-outbound bridge missing; `port_declarations` has no FK to `source_registrations`) and [B-071](bugs/open.md#B-071) (cross-org access requests structurally broken — both filed tonight as architectural findings pending PRD-session decisions; **both likely upgrade to Blocker post-Sunday**).

**Open Medium-severity bugs:** **3** — B-011 (OPA amd64-only on Apple Silicon), [B-060](bugs/open.md#B-060) (**verifier half closed via #150 — `seed:verify:soft-reset` now runs in CI**; remaining piece is the full-stack smoke-test CI integration), [B-062](bugs/open.md#B-062) (RLS-by-default — **design pass closed via [ADR-010](architecture/adr/ADR-010-rls-by-default.md) #158; step 1 closed via #161 + #168** — `agents.service.ts` load-bearing fix, ESLint `provenance/require-org-filter` rule at ERROR. Remaining ADR-010 steps: URL-param convention CI check, smoke-test cross-tenant layer).

**Open Low-severity bugs:** **8** — B-029, B-023, B-024, B-001, B-002, B-003, B-007, B-069 (B-069 partial-closed via #166: the inverted-UX `CONSUMPTION_GUIDANCE` template is gone, replaced by the per-port snippet picker; the rest of B-069's concept will be subsumed by the consumer-grade outbound reshape).

**Headline from the 2026-05-22 late session.** Nine PRs merged on top of the day's twelve. Two complete arcs landed:

*ADR-010 tenant-isolation backbone (PRs #161, #165, #167, #168):*
- #161 — `agents.service.ts` load-bearing fix per ADR-010 step 2. Every `findOne`/`find` on `agent_identities` and `agent_trust_classifications` now filters on `orgId = ctx.orgId`; raw audit-log queries in `getOversight` filter too; `listAgents` rejects 403 on `?orgId=` mismatch. 9 new tenant-isolation tests.
- #165 — service-org-filter audit doc. Walked every tenant-scoped service in `apps/api/src/`; 136 queries classified across 13 services. **85% already explicit-filtered; 11% Tier-3 (parent-FK-safety); zero Tier-4 real-leak gaps beyond what #161 already closed.** Mechanical cleanup punch list named.
- #167 — `eslint-plugin-provenance/` workspace package with custom rule `require-org-filter` at WARN level. Pattern-based detection of TypeORM repo calls missing `orgId` in `where`; magic-comment escape hatch (`// @cross-tenant-by-design: <reason>`).
- #168 — Tier-3 mechanical cleanup (slo + product-enrichment + notifications + trigger workers + connectors + invitations + preferences). Rule tightened to ERROR. **Next missing-orgId regression fails CI.**

*Consumer-grade outbound foundation (PRs #162, #163, #164, #166, #169):*
- #162 — consumer-grade outbound reframe doc. Names the framing: configuration brokerage (platform brokers configuration; user supplies own source-system identity) vs. credential brokerage (platform mints credentials). The right cut shrinks the lift from "3-5 months / per-source-type forever" to ~3-4 months. Embeds Matt's user story (Situations A/B/C, credential lifespan, Dehghani gap named).
- #163 — filed B-068 (marketplace cross-org URL break), B-069 (inverted `CONSUMPTION_GUIDANCE` UX), B-070 (inbound-outbound bridge missing).
- #164 — `@AllowCrossOrgRead` decorator + 9 marketplace-read endpoints decorated (trust-score, lineage upstream/downstream/impact, slo list/detail/evaluations/summary). **The marketplace's central use case — a consumer in Org A reading a product owned by Org B — now works. Verified end-to-end on dev: acme analyst on beta-industries trust-score returns 200; POST recompute still 403s.**
- #166 — per-port snippet generator + tool picker in the Ports tab. Backend `ConnectionPackageService.generateSnippetForPort(productOrgId, productId, portId, destination, requesterPrincipalId)` with 6 destinations (python real for all 6 interface types; dbt + sql_client + jdbc real for sql_jdbc; power_bi + tableau deferred). Frontend replaces the static `CONSUMPTION_GUIDANCE` placeholder with a dropdown that fetches real snippets on demand. **First concrete piece of the consumer-grade outbound reshape.** Verified on dev with `revenue-sql` port (after hand-fixing its seed-data shape to the canonical schema — see "Known limit" in PR #166).
- #169 — filed B-071 (cross-org access requests structurally broken). The write-side architectural counterpart to B-068. Three model options laid out (A: requester's-org owns / B: split lifecycle / C: marketplace-shared namespace). Recommend deferring code action to the PRD session.

**What this means for tomorrow's PRD overhaul.** The weekend input package is complete: connector lift survey (#157), consumer-grade outbound reframe (#162), service-org-filter audit (#165), per-port snippet generator (#166) as a working primitive, and four bug entries (B-063, B-068 closed, B-070, B-071) that frame the architectural questions. ADR-010 step 1 is fully closed end-to-end — the next missing-orgId regression fails at PR time, so the PRD session doesn't have to babysit tenant-isolation hygiene as it reshapes the framing. The decisions waiting for Sunday: framing commit (Power BI bar yes/no), credential model (configuration vs credential brokerage), B-071 Model A/B/C, B-070 bridge shape, B-063 scope (Options 1/2/3/4 reframed under the new lift math).

---

**Headline from the 2026-05-22 daytime session (preserved for context).** Twelve PRs merged. The arc has four halves:

*Claim-vs-code audit and reconciliation (PRs #148–#152):*
- #148 — claim-vs-code audit doc (`documents/audits/claim-vs-code-2026-05-22.md`). Walked every architectural claim in CLAUDE.md and every "✅ Complete" / "Implemented" entry referenced from the status board against actual code. 12 findings; none new-blocker. Most hollowness lives in the docs, not the codebase.
- #149 — Domain 9 P1 reconciliation. The status doc's table said all 4 P1 items were "Not implemented Blocker"; reality is 3 of 4 are end-to-end real (ownership, access status, column schema via declared port contracts), 1 partial (freshness signals — SLO-based real, lastRefreshedAt stub). Both source-of-truth docs reconciled.
- #150 — seed-verifier CI job. Closes the verifier half of B-060: every push to main + PRs touching migrations/seed run `pnpm seed:verify:soft-reset` against a freshly-migrated Postgres with a minimal fixture. Catches softReset SQL drift on the PR that introduces it.
- #151 — filed B-064/B-065/B-066/B-067 from the audit's candidate-bug list. Documentation-claim drift, not new security gaps.
- #152 — CLAUDE.md reconciliation closing B-064/B-065/B-067. Schema list now matches `\dt` output; TLS-at-Kong rule replaced with Caddy-on-MVP / Kong-on-Phase-6 tier-aware statement; agent-scope rule names the actual AQL `ConnectionReferenceGuard` layer. Plus `lineage.emission_log` added (was missing from the schema list entirely).

*Frontend bug fixes from the audit (PRs #153–#156):*
- #153 — B-066 legacy-ref UI badge. Amber card + "Legacy · non-renewable" pill + explanatory paragraph for refs with `causedBy='legacy_migration'`. Closes the inconsistency between CLAUDE.md's rule and the deferred Domain 12 UI item.
- #154 — B-005 hide-decommissioned in domain dashboard. Frontend filter + opt-in toggle with count.
- #155 — B-008 hide Request Access for product owner on dashboard view. Matches marketplace behavior. Flagged a related latent bug (`isOwner={product.status === 'draft'}` heuristic) without fixing — deferred to B-002.
- #156 — B-006 enforce contract schema on output ports at authoring time. Mirrors the backend `publishProduct` rule in the Add Port form.

*Strategic input for the weekend (PRs #157, #158):*
- #157 — connector lift survey (`documents/architecture/connector-lift-survey-2026-05-22.md`). Per-type estimates for the 9 unimplemented connector types against the Databricks ~8-hour actual. **Headline finding: ~41 hours best-case for all 9, ~62-86 hours realistic — not the 8-16 weeks the original B-063 entry quoted.** Surfaces Option 4 (incremental tranches) alongside Options 1/2/3.
- #158 — ADR-010 RLS-by-default design pass (Status: **Proposed**). Decision: formalize the explicit-`orgId`-filter pattern at the service layer as load-bearing; RLS stays as Phase 6 hardening backstop. Driven by sample: `agents.service.ts` is 2-of-13 org-filtered (vs products 13-of-13, governance 17-of-20) — that's the load-bearing fix when B-062 implementation lands.

*Frontend test infrastructure (PR #159):*
- #159 — Vitest + React Testing Library + jsdom set up from scratch (zero frontend tests existed before). First test file covers B-066's ReferencesTab rendering — 6 tests, ~23s CI run. Pattern documented for new contributors: extract leaf components, test them directly, avoid full-page mounts. New `test-web` CI job mirrors the `test-api` shape.

---

**Headline from the 2026-05-21 session (preserved for context).** Ten PRs merged. The arc has two halves:

*Operator tooling + tenant isolation cleanup (PRs #137–#140):*
- #137 — repaired `softReset` against the live schema, added `seed:verify:soft-reset` CLI verifier (B-060 part 1).
- #138 — rewrote `demo-smoke-test.sh` layers 3–6 against real org-scoped routes; behavioral tenant-isolation check replaced the phantom `/rls-probe` endpoint (B-060 part 2).
- #139 — filed B-061 after the smoke-test rewrite surfaced a cross-org information leak on `/governance/dashboard`, `/governance/effective-policies`, and `/products/:id/trust-score`.
- #140 — extended `JwtAuthGuard.canActivate` with a `request.params.orgId === request.user.orgId` check that throws 403 on mismatch. Five new unit tests, plus a new smoke-test layer that asserts cross-org URLs return 403. Cross-org leak closed.

*B-063 surfaced + Databricks integration Layers 1–4 (PRs #141–#146):*
- #141 — filed B-063 + `documents/architecture/databricks-integration-sketch.md` after realizing the connector framework is register-only beyond PG/S3.
- #142 — Layer 1: real Databricks probe against the SCIM `/Me` endpoint, plus a `local-env:` credential sentinel so any connector can be tested in local dev without AWS Secrets Manager.
- #143 — Layer 2: Unity Catalog schema inference (`GET /unity-catalog/tables/{full_name}`), three-part name validation.
- #144 — Layer 3a: operator-triggered discovery crawl via `POST /connectors/:id/crawl`. New `connectors.discovery_crawl_events` table (V30). Idempotent.
- #145 — Layer 3b: `connectors.capability_manifests` table (V31), seeded Databricks 1.0.0 manifest, read-only service, auto-crawl on connector registration (fire-and-forget, gated on the manifest's `supports_discovery_crawl`).
- #146 — Layer 4: table-level lineage projection from Unity Catalog Lineage Tracking API into Neo4j via the existing `LineageService`. Cross-catalog edges flowed through.

**Strategic correction at end of session.** Matt: *"This is not about demos. That's part of it. But for this to be real, every single one of those connectors needs to actually work. EVERY ONE. This is not open source ready. This coming weekend, we are going back to the drawing board and overhauling the damned PRD."* The session's framing — "ship Databricks as the demo beat" — was incomplete. The platform's credibility lives or dies on every advertised capability. Three connectors implemented out of twelve is not OSR; it's a working demo path with nine pieces of fakery around it. Captured in the new feedback memory `feedback_osr_means_every_connector_real.md`.

**What this means for the board going into the weekend.** "Active OSR blockers: 0" — the headline this board carried entering the 2026-05-21 session — was wrong by the standard above. B-063 was filed during that session and is now correctly marked as the sole open OSR Blocker.

The 2026-05-22 audit (#148) was supposed to test whether *other* CLAUDE.md / PRD claims were similarly hollow. They mostly weren't: 5 of 5 non-negotiable architectural constraints verified real, 9 of 10 operational rules verified real, audit log / JWT / Secrets Manager / RLS policies / connection-reference guard all real. The hollowness lives in the docs, not the codebase. **The category "PRD claims a thing the code doesn't do" turns out to be narrower than the B-060 → B-061 → B-063 arc made it look.**

The connector lift survey (#157) then reframed the B-063 strategic question. Original entry quoted 8-16 weeks for Option 1. Per-type breakdown comes to ~41 hours best-case, ~62-86 hours realistic — 1.5-2.5 person-weeks. **The engineering side just stopped being the gating factor on B-063; the Sunday decision turns on product/market/brand questions** (does the platform's identity stay "universal federated mesh" or shift to "the PG/S3/Databricks-first mesh that grew"). A possible Option 4 (incremental tranches over 3-4 weeks, shipping the connector count week-over-week) is in the doc.

---

## Up next (priority order)

**The biggest item is not on this list.** It's the 2026-05-24 weekend PRD overhaul. The roadmap below this section is the OLD plan from before B-063 was surfaced; it is preserved for context only. Expect the post-weekend status board to reshuffle this list substantially — possibly retire it entirely in favor of a new structure that takes the connector framework gap seriously.

In the meantime, these are the items that survive any reasonable PRD reshape:

1. **PRD overhaul session (2026-05-24 weekend).** What "OSR" actually means in light of the connector framework gap. Whether Phase 3 (claimed ✅ Complete) reopens or the phase model itself is revised. Whether the 12-connector promise is the right scope or whether OSR ships with a smaller, fully-implemented set. What other platform claims are similarly hollow. The output of this conversation reshapes everything else on this board.
2. **B-063 audit pass — does every connector type need to ship?** Real options: (a) implement all 12; (b) ship OSR with a documented smaller scope (e.g. "PG + S3 + Databricks v1, others marked Experimental"); (c) hide the registration form for unimplemented types until they're real. This is a strategy question, not a code question — answer in the weekend session.
3. **The B-060 → B-061 → B-063 pattern audit.** Three different "feature named in data model / PRD / type system but not implemented." Almost certainly more like this in the codebase. CLAUDE.md should be re-read line by line against the actual code; the PRD's "Implemented" / "Partially implemented" / "Not implemented" entries need a fresh pass. Don't trust any "✅" without verification.
4. **B-060 CI integration (last piece).** Wire `demo-smoke-test.sh` (now including the B-061 cross-org regression check) and `seed:verify:soft-reset` into a workflow that runs on every merge to main. Small, structural, prevents the next API/schema move from re-breaking either script.
5. **B-062 — RLS-by-default refactor.** Defense-in-depth for B-061. Design-pass work: per-request sticky connection, transactional wrapper, or formalize "every service query filters on `ctx.orgId` explicitly" as a lint-enforced pattern. Bigger lift, deserves its own session.
6. **B-011 — multi-arch OPA image.** Bigger jump than the entry suggests (OPA 1.x defaults to Rego v1; bootstrap policy at `infrastructure/docker/config/opa/policies/health.rego` is v0 syntax). Apple Silicon contributors run under amd64 emulation today — functional, slow.
7. **Low-severity backlog** — B-029, B-023, B-024, B-001-B-008.

---

## In flight

Nothing open. All 21 PRs from the 2026-05-22 sessions merged (#148–#159 daytime, #161–#169 late).

---

## Recently shipped (last 14 days)

| Date | PR | What |
|---|---|---|
| 2026-05-22 (late) | #169 | Filed B-071 — cross-org access requests structurally broken. Three model options for PRD-session decision. Write-side counterpart to B-068 (now closed) |
| 2026-05-22 (late) | #168 | ADR-010 step 1 closure — Tier-3 mechanical cleanup (slo + product-enrichment + notifications + trigger workers + connectors + invitations + preferences); `provenance/require-org-filter` ESLint rule tightened to ERROR. Next missing-orgId regression fails CI |
| 2026-05-22 (late) | #167 | `eslint-plugin-provenance/` workspace package + `require-org-filter` rule at WARN. Pattern-based AST detection of TypeORM repo calls missing `orgId` in `where`; magic-comment escape hatch (`// @cross-tenant-by-design: <reason>`). 17 RuleTester tests pinning behavior |
| 2026-05-22 (late) | #166 | Per-port snippet generator + tool picker (B-069 partial; consumer-grade outbound first piece). Backend `ConnectionPackageService.generateSnippetForPort` with 6 destinations (python all 6 interface types; dbt + sql_client + jdbc real for sql_jdbc; power_bi + tableau deferred). Frontend replaces `CONSUMPTION_GUIDANCE` placeholder with a dropdown |
| 2026-05-22 (late) | #165 | Service-layer org-filter audit doc (`documents/audits/service-org-filter-audit-2026-05-22.md`). 136 queries classified across 13 services; 85% explicit-filtered, 11% Tier-3, zero Tier-4 real leaks beyond what #161 closed |
| 2026-05-22 (late) | #164 | B-068 fix — `@AllowCrossOrgRead` decorator + 9 marketplace-read endpoints decorated (trust-score x2, lineage x3, slo x4). Marketplace cross-org reads work end-to-end. Verified on dev. Write/mutation endpoints stay same-org-only |
| 2026-05-22 (late) | #163 | Filed B-068 / B-069 / B-070 from the consumer-persona walkthrough |
| 2026-05-22 (late) | #162 | Consumer-grade outbound reframe doc — names configuration brokerage cut, embeds Matt's user story (Situations A/B/C), sizes ~3-4 months for tight first cut. Peer to #157 lift survey and #148 claim-vs-code audit |
| 2026-05-22 (late) | #161 | B-062 / ADR-010 step 2 — `agents.service.ts` load-bearing fix. Every `findOne`/`find` on agent entities now filters on `orgId = ctx.orgId`. `listAgents` rejects 403 on `?orgId=` mismatch. 9 new tenant-isolation tests; 735 → 738 / 738 passing |
| 2026-05-22 | #159 | Frontend test infrastructure — Vitest + React Testing Library + jsdom set up from scratch. First test file (`AgentDetailPage.test.tsx`, 6 tests) pins B-066 ReferencesTab rendering. New `test-web` CI job |
| 2026-05-22 | #158 | ADR-010 (Proposed) — RLS-by-default design pass closing B-062's architectural question. Decision: explicit-`orgId`-filter pattern at the service layer is load-bearing; RLS stays as Phase 6 backstop. Implementation deferred |
| 2026-05-22 | #157 | Connector lift survey — per-type estimates for the 9 unimplemented connector types. Reframes B-063 Option 1 from "8-16 weeks" to ~41-86 hours; floats Option 4 (incremental tranches) |
| 2026-05-22 | #156 | B-006 — enforce contract schema on output ports at authoring time. Frontend mirrors the backend `publishProduct` rule |
| 2026-05-22 | #155 | B-008 — hide Request Access button for product owner on dashboard view. Matches existing marketplace behavior. Adjacent `isOwner={status==='draft'}` latent bug flagged for B-002 |
| 2026-05-22 | #154 | B-005 — hide decommissioned products by default in domain dashboard. Frontend filter + opt-in toggle with count |
| 2026-05-22 | #153 | B-066 — legacy connection-reference UI now visually distinct. Amber card + "Legacy · non-renewable" pill + explanatory paragraph for refs with `causedBy='legacy_migration'` |
| 2026-05-22 | #152 | CLAUDE.md reconciliation closing B-064/B-065/B-067 — schema list matches `\dt`; TLS-at-Kong rule replaced with tier-aware statement; agent-scope rule names AQL `ConnectionReferenceGuard`. Added `lineage.emission_log` (was missing) |
| 2026-05-22 | #151 | Filed B-064/B-065/B-066/B-067 from the audit's candidate-bug list — documentation-claim drift |
| 2026-05-22 | #150 | B-060 part 1 (verifier half) — `seed:verify:soft-reset` CI job runs on push to main + migration/seed PRs. Catches softReset schema drift on the PR that introduces it |
| 2026-05-22 | #149 | Domain 9 P1 reconciliation — table claimed "Not implemented Blocker" on all 4 P1 items; reality is 3 of 4 end-to-end real, 1 partial. Both source-of-truth docs reconciled |
| 2026-05-22 | #148 | Claim-vs-code audit doc — 12 findings, none new-blocker. Verified the platform is more correct than the docs in most places |
| 2026-05-21 | #146 | B-063 Layer 4 — Databricks lineage projection from Unity Catalog Lineage Tracking API into Neo4j. Table-level only; column-level deferred to Layer 4b |
| 2026-05-21 | #145 | B-063 Layer 3b — `connectors.capability_manifests` table (V31) + read-only service + auto-crawl on connector registration |
| 2026-05-21 | #144 | B-063 Layer 3a — Databricks discovery crawl. `connectors.discovery_crawl_events` table (V30). Idempotent re-crawl. `POST /connectors/:id/crawl` |
| 2026-05-21 | #143 | B-063 Layer 2 — Databricks schema inference via Unity Catalog REST `/tables/{full_name}` |
| 2026-05-21 | #142 | B-063 Layer 1 — real Databricks connectivity probe + `local-env:` credential sentinel for laptop dev without AWS |
| 2026-05-21 | #141 | Filed B-063 + Databricks integration sketch after realizing the connector framework is register-only beyond PG/S3 |
| 2026-05-21 | #140 | B-061 fix — `JwtAuthGuard` compares URL `:orgId` to JWT claim, 403 on mismatch. Cross-org leak closed |
| 2026-05-21 | #139 | Filed B-061 — cross-org information leak on `/governance/dashboard`, `/governance/effective-policies`, `/products/:id/trust-score` |
| 2026-05-21 | #138 | B-060 part 2 — rewrote `demo-smoke-test.sh` layers 3–6 against real org-scoped routes + behavioral tenant-isolation check |
| 2026-05-21 | #137 | B-060 part 1 — repaired `softReset` against the live schema + new `seed:verify:soft-reset` CLI verifier |
| 2026-05-18 | #133 | B-050 — smoke-test layer 2 calls `/organizations/me`; switched to JWT-claim-derived org id + real `/organizations/:orgId` route; tightened claim-shape check |
| 2026-05-18 | #132 | B-056 — logout from `/agents` returned NestJS 404; stale `/agent` Vite proxy prefix-matched `/agents` and forwarded post-logout reloads to the API; removed |
| 2026-05-18 | #131 | B-058 — governance@acme receives the trust-score-drop notification; same payload, distinct `seedKey` |
| 2026-05-18 | #130 | B-059 — cross-domain approve/deny bypass; service-layer ownership check (`org_admin` short-circuit; otherwise must own product); audit row on scope violation |
| 2026-05-17 | #129 | B-057 — agent detail page at `/agents/:agentId` with Overview / Access Grants / Connection References / Classification History tabs |
| 2026-05-17 | #128 | File B-059 — access-request approve/deny missing product-ownership check |
| 2026-05-17 | #127 | B-055 — standalone Pending Access Requests page replaces the notification-only approver path |
| 2026-05-17 | #126 | Retract B-054 (misdiagnosed silent grant); expand B-055 |
| 2026-05-17 | #122–#125 | Demo asset inventory + mid-demo handoff status board |
| 2026-05-17 | #117–#121 | Demo environment polish (B-051/52/53 fixes, end-of-session housekeeping) |
| 2026-05-17 | #113 | Persistent EBS for Caddy certs (B-049) — kills the LE rate-limit problem |
| 2026-05-17 | #112 | Server-side `/seed/principals` Keycloak attribute write — B-048 |
| 2026-05-16 | #104–#110 | Demo environment marathon — persistent EIP + DNS, Caddy unification, demo-bootstrap and demo-sync fixes, Phase 4 MCP restoration |
| 2026-05-15 | #97–#101 | F7.46 follow-ons + onboarding wizard + compose env-validator CI |
| 2026-05-14 | #92 | F7.46 Onboarding Experience — first-run wizard with progress persistence |
| 2026-05-13 | #90, #91 | F7.7 Role Assignment UI + F7.22/F10.4 Domain Team Management |
| 2026-05-13 | #77–#86 | Domain 12 runtime enforcement — 9-PR arc |

(Older shipped work — see `prd/implementation-status.md`.)

---

## Demo environment — current state

- ✅ Persistent EIP `34.204.222.196` tagged `provenance-demo-eip`; Cloudflare A records permanent.
- ✅ Persistent Caddy-data EBS volume `vol-0fd2383ae142d2c95` (1 GB gp3, us-east-1c) — survives `terraform destroy`. Real Let's Encrypt prod certs in the volume, valid through 2026-08-15.
- 🟡 **Demo instance `i-0669a177766d2a29e` state at end of 2026-05-22 session: unverified, even more behind.** Last known running commit was `01147d0` (2026-05-18); current main has the 2026-05-21 session's 10 PRs plus the 2026-05-22 session's 12 PRs on top. **Anyone running a demo before the 2026-05-24 weekend overhaul should `demo-sync.sh main` first** to pull both nights' changes. Expect the new Databricks connector type to surface in the UI (register one for the discovery / lineage story), the agent detail page changes (B-066 legacy badge), the dashboard changes (B-005 decommissioned toggle, B-008 owner-vs-non-owner Request Access), and the Add Port validation (B-006). **Stop the instance if you're not actively using it** — `aws ec2 stop-instances --instance-ids i-0669a177766d2a29e`. Cost ~$0.10/day stopped vs ~$0.16/hour running.
- 🟡 **Dev box `/opt/provenance` state: also on the 2026-05-15 commit `58741b6`.** Same pattern — the 2026-05-22 session worked entirely against current main without redeploying the dev box. Visual verification of #153/#154/#155/#156 was explicitly deferred (called out in each PR body) — the dev box can't show those changes until it pulls forward. Tracking as a deploy/test plan item, not a merge gate.
- ✅ **`demo-reset.sh --soft` and smoke test layers 3–6 now work** (B-060 parts 1+2 closed tonight). The remaining B-060 piece is wiring the smoke test into CI so this kind of drift surfaces on the next API/schema move.
- ⚠️ **For a Databricks demo specifically:** the `DATABRICKS_DEV_TOKEN` env var passthrough exists in `docker-compose.ec2-dev.yml` (PR #142). On the demo box, you'll need to set that env var before the api container picks up the value — see `infrastructure/docker/.env.example` for the documented pattern. The token in any operator's chat / shell history should be considered burned after use; rotate from Databricks User Settings → Developer → Access tokens.

**Restart procedure** (next time you start cold):

1. `aws ec2 start-instances --instance-ids i-0669a177766d2a29e` — wait ~2 min, confirm `https://demo.provenancelogic.com/api/v1/health` returns 200.
2. `ssh -i ~/.ssh/provenance-demo ec2-user@34.204.222.196 'cd /opt/provenance && git pull --ff-only origin main'` — pulls the 10 PRs from the 2026-05-21 session AND the 12 from 2026-05-22.
3. `ssh -i ~/.ssh/provenance-demo ec2-user@34.204.222.196 'bash /opt/provenance/infrastructure/scripts/demo-sync.sh main'` — pulls + restarts + migrates (V30 + V31 will apply on a stale demo box) + reseeds + runs smoke test. Smoke test should now pass all six layers thanks to B-060.
4. Run the demo. Stop the instance when done.

---

## Deferred to post-launch (no shame)

- **F7.29 Access Request SLA + escalation** — notifications fire; no auto-escalation timer.
- **F7.42 Human Review Queue** — only relevant when Supervised agents are in active use.
- **Phase 5.5 Anomaly Detection** — additive observability.
- **Phase 5.7 SOC 2 Foundations** — enterprise track.
- **Phase 6 (Production Scale)** — Kubernetes, managed AWS services, SOC 2 Type II audit. Triggered by enterprise customers or funding, not a calendar date.
- **Demo "production-mode images" hardening** — the architectural follow-on to the B-051/52/53 paper-cut family.
- **Public-facing "sexy" landing page at `provenancelogic.com`** — Matt's wife flagged 2026-05-17. Marketing surface for investor walkthroughs.

---

## Phase summary

| Phase | Scope | Status |
|---|---|---|
| 1 | Foundations | ✅ (verified) |
| 2 | Governance + marketplace | ✅ (substantively shipped; depth of "Implemented" entries TBD in the weekend re-audit) |
| 3 | Lineage + trust + **discovery** | ⚠️ **PRD claims ✅; reality is partial.** Lineage emission, trust score, and the discovery FRAMEWORK exist. Discovery for actual connector types: PostgreSQL ✅, S3 ✅, Databricks ✅ (Layers 1-4 shipped 2026-05-21). dbt, Snowflake, Fivetran, and all other priority connectors: ❌ Not implemented. The "✅" on this row is being preserved on this board only as a literal echo of the PRD; the weekend overhaul will reconcile. |
| 4 | MCP + agents + semantic search | ✅ (UI surface caught up with B-057's agent detail page) |
| 5 | Open Source Ready | ❌ **Blocked on B-063.** The previous board entry ("no active OSR blockers") was wrong. The connector framework gap is OSR-blocking by Matt's stated standard: every advertised connector must actually work. |
| 6 | Production Scale | 🔲 When funded / customer-triggered |

Domain 12 (Connection References) was cross-cutting; closed at the enforcement layer on 2026-05-13. UI surface for connection references shipped with B-057.

---

## Links

- `prd/osr-roadmap.md` — full roadmap with stage definitions
- `prd/implementation-status.md` — authoritative per-feature status
- `bugs/open.md` — open bug ledger
- `bugs/resolved.md` — searchable log of past root causes
- `runbooks/demo-environment.md` — demo EC2 procedure
- `demo-scripts/demo-asset-inventory.md` — grounded reference for what's demo-able on the seeded environment
- `architecture/adr/ADR-010-rls-by-default.md` — RLS design pass (B-062), Proposed status
- `architecture/connector-lift-survey-2026-05-22.md` — per-type lift estimates for the 9 unimplemented connectors (B-063 weekend input)
- `audits/claim-vs-code-2026-05-22.md` — the audit that drove the 2026-05-22 session
- `CLAUDE.md` — assistant context (architecture, patterns, constraints — *not* status)
