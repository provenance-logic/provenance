# Provenance — Status Board

> **What this is.** A one-screen snapshot of where Provenance actually stands. Read this *before* anything else when you sit down for a session. Regenerated by Claude at session end, or on demand. The authoritative per-feature detail lives in `prd/implementation-status.md`; this file is the dashboard view.

**Last updated:** 2026-05-17 (post-housekeeping; EC2 boxes being stopped overnight pending Let's Encrypt rate-limit reset at ~11:25 UTC)

---

## Right now

**Stage:** Stage 5 (Pre-launch sweep) — heading toward `v0.1.0-osr`.
**Active OSR blockers:** **zero**. Last blocker (B-048) closed by [#112](https://github.com/provenance-logic/provenance/pull/112).
**Open Medium-severity bugs:** **none**.
**Open Low-severity bugs:** 12 — B-001, B-002, B-003, B-005, B-006, B-007, B-008, B-011, B-023, B-024, B-029, **B-050** (new today; smoke-test calls a non-existent `/organizations/me` route — see [#114](https://github.com/provenance-logic/provenance/pull/114)). None gate launch.

**Headline from today's arc:** went from "B-048 just landed, let's verify on a fresh demo cycle" through "Let's Encrypt rate-limit wall hit; demo TLS-broken in browsers" to "persistent-EBS architecture designed, implemented, and **proved end-to-end across a destroy → apply → destroy → apply cycle**" in one focused session. Caddy's `instance.uuid` survived byte-identical across cycles. The wall is gone forever after one prod cert lands tomorrow morning UTC. Three PRs in flight from today: [#112](https://github.com/provenance-logic/provenance/pull/112) (merged — B-048 fix), [#113](https://github.com/provenance-logic/provenance/pull/113) (open — persistent EBS), [#114](https://github.com/provenance-logic/provenance/pull/114) (open — B-050 doc).

---

## Up next (priority order)

1. **Tomorrow ~11:25 UTC (after LE rate-limit reset):** merge [#113](https://github.com/provenance-logic/provenance/pull/113); start demo box (or destroy + apply if state diverged); first prod cert lands into the persistent EBS volume; browser-trusted padlock from that point forward. Optionally also merge [#114](https://github.com/provenance-logic/provenance/pull/114) (B-050 doc) and this status-board PR.
2. **Then re-run smoke-test layers 2–6** against the prod-cert demo cycle. Layer 2 should pass (B-048 fix is on main); layer 2 step 3 will fail on B-050 (`/organizations/me` route doesn't exist) — fix path documented in the bug entry. Layers 3–6 not yet exercised.
3. **Stage 5 polish** — README polish · doc link-check · "you are here" doc nav · clean-checkout smoke-test · status badges · tag `v0.1.0-osr` + release notes. Est. 2–3 days.
4. **Demo environment hardening follow-ups** — `demo-bootstrap.sh` should fail loudly if agent-query isn't healthy within a timeout (the silent-failure pattern that hid B-045 for weeks); document anonymous-volume staleness gotcha in operations runbook. Small but worth doing before the next contributor hits the same trap.
5. **(Post-launch, "no shame" deferrals)** F7.29 SLA escalation · F7.42 Human Review Queue · 5.5 Anomaly Detection · 5.7 SOC 2 Foundations · Phase 6 managed-AWS.

---

## In flight

- **[#113](https://github.com/provenance-logic/provenance/pull/113) — Persistent EBS for Caddy cert state.** CI green, awaiting merge. Architecture verified end-to-end on the live demo box today (destroy → apply → destroy → apply preserved Caddy's `instance.uuid` byte-identical). Resolves [B-049](bugs/resolved.md#b-049). Pre-allocated EBS volume `vol-0fd2383ae142d2c95` (1 GB gp3, us-east-1c, tagged `provenance-demo-caddy-data`) already exists; runbook updated.
- **[#114](https://github.com/provenance-logic/provenance/pull/114) — B-050 doc.** Smoke-test layer 2 step 3 calls a route the API doesn't expose. Doc-only; no fix in this PR — entry suggests fixing the smoke test rather than adding API surface.

Both EC2 boxes (dev + demo) being stopped overnight pending tomorrow's LE rate-limit reset; both will start back up clean from the persistent EIP / persistent EBS pattern.

---

## Recently shipped (last 14 days)

| Date | PR | What |
|---|---|---|
| 2026-05-17 | #112 | Server-side `/seed/principals` Keycloak attribute write — B-048 (last OSR blocker) |
| 2026-05-16 | #110 | Bootstrap prebuilds packages/types + smoke-test paths (`/api/` → `/api/v1/`) — B-047 + B-043 |
| 2026-05-16 | #109 | **Phase 4 MCP server restored on dev after a 4+ week silent outage** — B-045 + B-046 |
| 2026-05-16 | #108 | Smoke-test health path + `.env.example` SEED_* dedupe — B-043 + B-044 |
| 2026-05-16 | #107 | Six demo-bootstrap / demo-sync fixes — B-037 through B-042 |
| 2026-05-16 | #106 | One Caddy for dev and demo, env-templated hostnames — B-036 |
| 2026-05-16 | #105 | Five fixes from first demo dry-run — B-031 through B-035 |
| 2026-05-16 | #104 | **Persistent EIP + Cloudflare DNS architecture for demo** — B-030 |
| 2026-05-15 | #101 | Docs housekeeping after F7.46 follow-on session |
| 2026-05-15 | #100 | Sample-data button (B-027) |
| 2026-05-15 | #99 | Compose env-validator CI + 3 latent drift fixes (B-028 fix 3) |
| 2026-05-15 | #98 | F7.46 follow-on — connector registration UI (B-025) |
| 2026-05-15 | #97 | F7.46 follow-on — agents registration UI (B-026) |
| 2026-05-15 | #96 | `refresh-ec2-dev.sh` (B-028 fix 2) |
| 2026-05-14 | #92 | **F7.46 Onboarding Experience** — first-run wizard with progress persistence |
| 2026-05-13 | #91 | **F7.22 / F10.4 Domain Team Management** completion |
| 2026-05-13 | #90 | **F7.7 Role Assignment UI** — org-level Roles page, audit + Keycloak sync |
| 2026-05-13 | #77–#86 | **Domain 12 runtime enforcement** — 9-PR arc; `CONNECTION_REFERENCE_ENFORCEMENT_ENABLED=true` default |

(Older shipped work, including Domain 11, F5.15 lineage viz, Domain 10 Workstream B, Phase 5.6 developer-experience arc — see `prd/implementation-status.md`.)

---

## Demo environment — current state

- ✅ Persistent EIP `34.204.222.196` tagged `provenance-demo-eip`; Cloudflare A records for `demo.provenancelogic.com` + `auth-demo.provenancelogic.com` permanent
- ✅ Persistent Caddy-data EBS volume `vol-0fd2383ae142d2c95` (1 GB gp3, us-east-1c, tagged `provenance-demo-caddy-data`) — created today; survives `terraform destroy`; cert persistence proved across a full destroy → apply cycle on the live demo box (Caddy `instance.uuid` survived byte-identical)
- ✅ `terraform plan/apply/destroy` clean against live AWS; both persistent resources preserved across destroy
- ✅ User-data installs Docker, Node 22, pnpm, buildx; clones repo at target SHA; mounts Caddy EBS at `/var/lib/caddy-data` with `sync`; runs bootstrap
- ✅ Bootstrap + sync run with `-f docker-compose.ec2-dev.yml -f docker-compose.demo.yml` so Caddy's `caddy_data` volume bind-mounts to the persistent EBS
- ✅ All 17 compose services come up healthy
- ✅ `demo-sync.sh main` completes seed across all sections; B-048 verified end-to-end (JWTs carry all three `provenance_*` claims)
- ✅ Smoke-test layer 1 (infrastructure) — API health 200, Keycloak OIDC 200, no unhealthy containers
- 🔲 Smoke-test layer 2 (auth) — needs re-run on fresh prod-cert cycle to confirm pass (B-048 fix is on main; expected to pass except for layer-2-step-3 which trips on B-050)
- 🔲 Smoke-test layers 3–6 — not yet exercised
- 🔄 Public HTTPS — Caddy currently has no usable cert (LE prod rate-limited until ~11:25 UTC tomorrow; staging fallback didn't materialize). External browser access returns `ERR_SSL_PROTOCOL_ERROR`. Box being stopped overnight; tomorrow's apply gets a real prod cert in one shot.

Runbook: `documents/runbooks/demo-environment.md` (updated today with EBS prereq). Terraform: `infrastructure/terraform/demo/`. Per-cycle cost: ~$0.08 of t3.xlarge compute for a ~30-min cycle, plus ~$0.10/month for the persistent EBS volume.

---

## Deferred to post-launch (no shame)

- **F7.29 Access Request SLA + escalation** — notifications fire; no auto-escalation timer. Acceptable v1.
- **F7.42 Human Review Queue** — only relevant when Supervised agents are in active use.
- **Phase 5.5 Anomaly Detection** — additive observability.
- **Phase 5.7 SOC 2 Foundations** — enterprise track.
- **Phase 6 (Production Scale)** — Kubernetes, managed AWS services, SOC 2 Type II audit. Triggered by enterprise customers or funding, not a calendar date.

---

## Phase summary

| Phase | Scope | Status |
|---|---|---|
| 1 | Foundations | ✅ |
| 2 | Governance + marketplace | ✅ |
| 3 | Lineage + trust + discovery | ✅ |
| 4 | MCP + agents + semantic search | ✅ (restored 2026-05-16 after a silent 4+ week regression — see B-045) |
| 5 | Open Source Ready | 🔄 Stage 5 polish — zero blockers |
| 6 | Production Scale | 🔲 When funded / customer-triggered |

Domain 12 (Connection References) was cross-cutting; closed 2026-05-13.

---

## Links

- `prd/osr-roadmap.md` — full roadmap with stage definitions
- `prd/implementation-status.md` — authoritative per-feature status
- `bugs/open.md` — open bug ledger
- `bugs/resolved.md` — searchable log of past root causes
- `runbooks/demo-environment.md` — demo EC2 procedure
- `CLAUDE.md` — assistant context (architecture, patterns, constraints — *not* status)
