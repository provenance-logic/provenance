{
  "name": "eslint-plugin-provenance",
  "version": "0.1.0",
  "private": true,
  "description": "Custom ESLint rules for the Provenance platform — load-bearing tenant-isolation pattern enforcement (ADR-010).",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  },
  "peerDependencies": {
    "eslint": ">=8.0.0"
  }
}
